import Spinner from "@/app/_components/Spinner";

export default function Loading() {
  return <Spinner />;
}

// this will be replace with the content of the page (layout still be the same) and as soon as the page rendered on the server will be replace with the loading content
